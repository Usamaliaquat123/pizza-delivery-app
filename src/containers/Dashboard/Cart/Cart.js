import React, { Component } from 'react';
import { View, Text } from 'react-native';

class Cart extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> Cart </Text>
      </View>
    );
  }
}

export default Cart;
